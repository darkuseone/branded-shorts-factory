# .state/

Runtime channel state that should survive across builds.

* `meme-history.json` — which videos already carried a meme, so the §11
  frequency gate (~1 of 8–12) stays honest on GitHub Actions.
