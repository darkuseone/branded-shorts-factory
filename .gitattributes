# Keep large stock libraries on LFS if present, but NEVER pointerize
# committed job avatars / b-roll — GHA HyperFrames needs real MP4 bytes.
*.mp4 filter=lfs diff=lfs merge=lfs -text
jobs/**/*.mp4 -filter -diff -merge text=auto
jobs/**/*.webm -filter -diff -merge text=auto
